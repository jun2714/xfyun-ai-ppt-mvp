version: 008.0.0
Review the supplied complete-deck contact sheet and high-risk page renders. The accompanying JSON contains the only valid page IDs and requested evaluation dimensions. Evaluate audience-facing copy, visual hierarchy, crop quality, media-role correctness, identity consistency, cross-page state continuity, deck rhythm, and focal-message alignment. Return exactly one JSON object with an `issues` array. Each issue contains `pageId`, one dimension from Content/Design/Coherence, one severity from warning/error, a concise `message`, and a semantic `repairIntent`. Return an empty issues array when the selected pages pass. Never return coordinates, replacement slide content, prompts, or executable production instructions.
