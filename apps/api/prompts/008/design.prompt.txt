version: 008.0.0
Return exactly one JSON object with `plan` and `intents`. Design the complete deck through semantic hierarchy, relationships, visual grammar, media identity, and cross-page constraints. Preserve linked content identities and declare only the sources allowed to change, appear, or remain concealed. Media descriptions are private generation metadata and must never be repeated in audience-visible content. Use generic visual and relationship language only. Do not return coordinates, sizes, templates, layout identifiers, page-role templates, executable code, CSS, HTML, SVG, or PPTX code.

Generated media is optional. Request it only when native text, shapes, connectors, and charts cannot communicate the page's central meaning. Never request an image merely as decoration. Prefer exact reuse of one deck-wide identity over repeated or near-duplicate generation, and declare controlled variants only when the narrative visibly changes that same entity.

`plan` contains: `briefId`, `designSeed`, `tone`, typography families and weights, a semantic palette with background/surface/text/primary/secondary/accent/muted colors, `visualGrammar`, `densityTarget`, rhythm, consistencyRules, crossPageConstraints, and assetIdentities. Visual grammar contains typographyCharacter, shapeVocabulary, motifRules, mediaLanguage, and variationPolicy. Shape forms may only be rectangle, rounded-rectangle, circle, line, arc, or freeform. Media roles may only be full-bleed-background, scene, subject, transparent-cutout, detail, or evidence.

Each intent contains: `pageId`, `focalMessage`, hierarchy, design groups, semantic relationships, visualStrategy, balance, flow, density, emphasis, mediaRequests, and avoid. Every media request contains a unique request `id`, an existing asset `identityId`, semanticEntityId, claimIds, role, private visual description, optional audience-facing `audienceAlt`, fit, focalPolicy, optional textSafeArea, required, visualIdentityKey, optional variantIntent, and reusePolicy. Valid reuse policies are exact, controlled-variant, and single-use. Every required content group must be referenced by hierarchy or a design group.

Type requirements are strict: `tone`, `consistencyRules`, `rhythm.continuity`, `crossPageConstraints`, `assetIdentities`, `hierarchy`, `groups`, `relationships`, `emphasis`, `mediaRequests`, and `avoid` are arrays. Every hierarchy item is an object with `contentGroupId` and integer `priority` from 1 to 5. Every group is an object with `id`, `contentGroupIds`, and `treatment`. If the Brief forbids images, set `assetIdentities` and every `mediaRequests` to empty arrays and use `visualStrategy: "none"` or `"relationship"`.

The exact required `plan` structure is: `briefId`, `designSeed`, `tone`, `typography` (`character`, `headingFamily`, `bodyFamily`, `headingWeight`, `bodyWeight`), `palette` (`mood`, `background`, `surface`, `text`, `primary`, `secondary`, `accent`, `muted`; every color is `#RRGGBB`), `visualGrammar`, `densityTarget`, `rhythm`, `consistencyRules`, `crossPageConstraints`, and `assetIdentities`. `visualGrammar` requires `typographyCharacter`; `shapeVocabulary` as an object with `character`, `forms`, `strokeStyle`, `cornerStyle`; `motifRules` as objects with `id`, `intent`, `frequency`, `layer`; `mediaLanguage` as an object with `rendering`, `backgroundTreatment`, `subjectTreatment`, `consistencyRule`; and `variationPolicy` as an object with `continuityStrength`, `diversityStrength`.

Use only these enum values: `densityTarget`: airy/balanced/dense; `rhythm.variation`: subtle/moderate/strong; shape `forms`: rectangle/rounded-rectangle/circle/line/arc/freeform; `strokeStyle`: none/subtle/expressive; `cornerStyle`: sharp/soft/round; motif `frequency`: rare/occasional/recurring; motif `layer`: background/support/accent; continuity/diversity strength: low/medium/high. Each intent must use the exact key `groups` (never `designGroups`) and `relationships` (never `semanticRelationships`). Intent enums: group `treatment`: plain/emphasis/paired/progressive/evidence/callout; relationship `kind`: sequence/contrast/supports/reveals/belongs; `visualStrategy`: none/full-bleed-background/scene/subject/collection/relationship; `balance`: symmetric/asymmetric/centered/directional; `flow`: vertical/horizontal/radial/sequence/free-emphasis; `density`: low/medium/high. Every emphasis item is an object with `targetId`, `strength` (low/medium/high), and `reason`.
