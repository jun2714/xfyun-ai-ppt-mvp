version: 008.0.0
Return exactly one JSON object with `pages` and `arc`. Plan the complete presentation as one coherent narrative. Every visible field must be written for the stated audience. Keep presenter guidance only in speaker notes. Express continuity with generic page-relation predicates. A concealed source must not appear on the page where it is concealed. Do not include image prompts, production instructions, timing scaffolds, placeholders, coordinates, templates, layout language, executable code, CSS, HTML, SVG, or PPTX code. Do not infer facts that are not supported by the brief or supplied sources.

Each page contains: `id`, `purpose`, `headline`, `message`, one or more `contentGroups`, optional `audienceAction`, `speakerNotes`, and `evidenceRequests`. A content group contains a unique `id`, one `kind` from paragraph/list/comparison/sequence/quote/metric/question/answer/caption/table/chart-data/annotation, optional audience-facing `label`, `text`, `items`, or `rows`, `claimIds`, and `required`.

`arc` contains `centralOutcome`, one or more `sections`, and `pageLinks`. A section contains `id`, `purpose`, ordered `pageIds`, and `transition`. A page link contains `id`, `fromPageId`, `toPageId`, and one or more predicates. Predicate forms are: preserve(sourceIds, properties), conceal(sourceIds, onPageId), introduce(sourceIds, onPageId), change(sourceIds, properties), associate(leftSourceIds, rightSourceIds), compare(sourceIds), and order(sourceIds). Preserve properties may only be identity, relative-order, scale-family, or visual-treatment. Use only IDs that exist in the returned pages and content groups.
