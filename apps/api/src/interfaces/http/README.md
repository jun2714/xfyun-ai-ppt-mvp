# HTTP interface

Routes, request parsing, presenters and middleware belong here. HTTP handlers may call application use cases but must not contain domain rules or instantiate infrastructure adapters.
