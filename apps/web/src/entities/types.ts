export type Brief = { id: string; title: string; audience: string; ageRange?: string; usageContext: string; objective: string; pageCount: number; constraints: string[]; revision: number };
export type ContentGroup = { id: string; kind: string; label?: string; text?: string; items?: string[]; rows?: Array<Array<string | number>>; claimIds: string[]; required: boolean };
export type OutlinePage = { id: string; purpose: string; headline: string; message: string; contentGroups: ContentGroup[]; speakerNotes: string[]; evidenceRequests: unknown[] };
export type PageRelationPredicate = Record<string, unknown> & { kind: string };
export type NarrativeArc = { centralOutcome: string; sections: Array<{ id: string; purpose: string; pageIds: string[]; transition: string }>; pageLinks: Array<{ id: string; fromPageId: string; toPageId: string; predicates: PageRelationPredicate[] }> };
export type Outline = { revision: number; confirmedAt: string | null; pages: OutlinePage[]; arc: NarrativeArc };
export type Bounds = { x: number; y: number; width: number; height: number };
export type SceneNode = { id: string; kind: "text" | "shape" | "image" | "chart" | "connector" | "group"; bounds: Bounds; zIndex: number; style: Record<string, unknown>; content: Record<string, unknown>; locked: boolean };
export type ScenePage = { id: string; width: number; height: number; background: string; nodes: SceneNode[]; selectedCandidateId: string; alternativeCandidateIds: string[]; pageLinkIds: string[]; riskFlags: string[] };
export type Scene = { revision: number; theme: Record<string, unknown>; pages: ScenePage[] };
export type QualityIssue = { code: string; severity: "warning" | "error"; message: string; pageId?: string; nodeIds: string[] };
export type Quality = { passed: boolean; visualReviewStatus: string; scores: Record<string, number>; issues: QualityIssue[] };
export type PresentationState = { brief: Brief; outline?: Outline; design?: unknown; assetBundle?: unknown; renderEvidence?: unknown; scene?: Scene; quality?: Quality };
export type Job = { id: string; status: "queued" | "running" | "succeeded" | "failed"; stage: string; progress: number; error: { message: string } | null };
