{
  "name": "sparkdeck",
  "version": "0.2.0",
  "private": true,
  "packageManager": "pnpm@10.23.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:web": "pnpm --filter @sparkdeck/web dev",
    "dev:engine": "node scripts/dev-engine.mjs",
    "setup:engine": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/setup-engine.ps1",
    "build": "pnpm --filter @sparkdeck/web build && npm --prefix engine/editor run build",
    "typecheck": "pnpm --filter @sparkdeck/web typecheck && npx --prefix engine/editor tsc --noEmit -p engine/editor/tsconfig.json",
    "verify": "pnpm build && pnpm typecheck && pnpm test",
    "test": "npm --prefix engine/editor test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
