# 009 实施任务与证据

所有任务初始状态均为未完成。代码存在不代表完成，只有证据文件通过并经用户确认后才能勾选。

## Phase 0：停止错误完成声明

- [ ] 0.1 将 008 标记为未完成，保留已有技术证据但不得继续当作真实成品证据。
- [ ] 0.2 建立 008 已实现、部分实现、未证明、失败四类清单。
- [ ] 0.3 记录验收脚本 `sharp` 依赖解析失败和零模型调用证据。
- [ ] 0.4 用户确认 009 Spec 后才允许修改生产代码。

证据：差距矩阵、失败日志、用户确认记录。

## Phase 1：交付入口和零费用预检

- [ ] 1.1 为验收器建立独立 package 或将其放入明确拥有依赖的 workspace。
- [ ] 1.2 增加真实 import/启动预检，禁止只用 `node --check` 代替运行检查。
- [ ] 1.3 检查 Prompt、字体、输出目录、WPS、PowerPoint 和模型配置。
- [ ] 1.4 预检失败时证明文本、图片和视觉调用均为零。
- [ ] 1.5 验收 Brief 改为从仓库外部 JSON 读取，执行器不保存固定主题和固定文案。

证据：preflight-report.json、零费用 usage ledger、冷启动日志。

## Phase 2：真实模型协议验证

- [ ] 2.1 使用真实文本模型执行 Narrative Contract，保存脱敏结构报告。
- [ ] 2.2 使用真实文本模型执行 Design Contract，保存脱敏结构报告。
- [ ] 2.3 覆盖非 JSON、Markdown、截断、页数错误、重复 ID、错误引用和制作语言。
- [ ] 2.4 覆盖标题空洞、标题与正文重复、图片描述泄漏和答案提前泄漏。
- [ ] 2.5 对候选文本模型执行同一协议、同一外部 Brief 的成功率和费用对比。
- [ ] 2.6 任何失败均不自动付费重试、不静默换模型。

证据：model-contract-report.json、usage ledger、失败码明细。

## Phase 3：去除隐藏结构写死

- [ ] 3.1 删除所有页面必经的固定“标题带 + 内容带”骨架。
- [ ] 3.2 标题作为语义节点进入通用关系求解。
- [ ] 3.3 扫描并审查内容数量、方向和强调值直接选择固定树的分支。
- [ ] 3.4 增加无标题带、标题与媒体协同、标题与数据协同等由约束产生的结构测试。
- [ ] 3.5 保留工程上限和安全区等合法阈值，并集中配置和说明依据。
- [ ] 3.6 两次独立扫描确认无业务写死和无单一通用骨架写死。

证据：AST 报告、构图 Trace、结构多样性报告、生产 diff。

## Phase 4：正常 PPT 原生设计能力

- [ ] 4.1 建立可组合 Typography、Surface、Badge、Marker、Divider 和 Connector。
- [ ] 4.2 支持富文本层级、段落、项目符号、局部强调和可靠中文字体测量。
- [ ] 4.3 支持比较、顺序、指标、图表注释等通用语义组合，不建立页面角色模板。
- [ ] 4.4 支持图片蒙版、保护层、焦点裁剪和一致的图片处理。
- [ ] 4.5 Deck 级视觉语法保证一致性，同时允许真实页面轮廓变化。
- [ ] 4.6 用外部未知内容验证设计能力，不把测试主题加入生产判断。

证据：Scene Graph、逐页 PNG、可编辑 PPTX、构图 Trace。

## Phase 5：素材正确性

- [ ] 5.1 证明布局选定前图片调用为零。
- [ ] 5.2 增加语义相关性、身份一致性和适龄性校验。
- [ ] 5.3 增加背景、场景、主体、抠图、细节和证据角色的放置检查。
- [ ] 5.4 增加 MIME、重定向、超时、损坏图片和下载安全测试。
- [ ] 5.5 联系表同时包含高风险页高清图和身份对照。
- [ ] 5.6 所有图片调用都能对应最终 Scene 引用；未使用调用数为零。
- [ ] 5.7 增加 Deck 级图片上限和后台成本阻断，不增加前台费用确认。

证据：asset-report.json、身份对照图、usage ledger、失败注入报告。

## Phase 6：事务、并发和持久化

- [ ] 6.1 为所有长 Job 增加输入 revision/hash capture 和 CAS commit。
- [ ] 6.2 大纲、设计、素材、质量、视觉和导出分别执行并发覆盖测试。
- [ ] 6.3 usage 使用独立追加事务，业务提交冲突不丢费用。
- [ ] 6.4 文件仓储明确为开发实现；生产仓储方案必须另行经用户确认。
- [ ] 6.5 进程中断、磁盘写入失败和旧 Job 恢复均不产生永久 running 或脏状态。

证据：concurrency-report.json、restart-report.json、持久化故障测试。

## Phase 7：编辑器完整性

- [ ] 7.1 浏览器 E2E 验证创建、大纲确认、生成、预览、编辑和导出。
- [ ] 7.2 实现并验证主流 PPT 基础操作和键盘交互。
- [ ] 7.3 实现本地图片上传、焦点裁剪、图层和富文本。
- [ ] 7.4 新增页面同步 Narrative/Design，或明确标记为手工页面并限制重新设计。
- [ ] 7.5 generated base 与 user override 可重放、可合并、可冲突提示。
- [ ] 7.6 错误提示保留编辑上下文并给出明确恢复动作。

证据：浏览器录像/截图、命令日志、编辑后 PPTX 对比。

## Phase 8：完整质量证据

- [ ] 8.1 每页执行内容、字体、碰撞、裁剪、图片角色和制作语言规则。
- [ ] 8.2 每个关联组执行 conceal/introduce/preserve/change 状态验证。
- [ ] 8.3 输出每页 WPS 和 PowerPoint PNG，不只保存平均差异分。
- [ ] 8.4 局部高风险区域能够定位到节点，不能被全页平均值掩盖。
- [ ] 8.5 一次视觉调用同时审查整稿、高风险页和素材身份。
- [ ] 8.6 人工逐页验收记录明确通过或拒绝理由。

证据：quality-report.json、render-evidence.json、逐页 PNG、visual-review.json、human-review.md。

## Phase 9：三份真实外部基准稿

- [ ] 9.1 用户或外部文件提供此前未见的幼儿课堂 Brief。
- [ ] 9.2 第一份从冷启动生成，失败立即冻结，不修改代码。
- [ ] 9.3 用户确认第一份后，执行此前未见的家长沟通 Brief。
- [ ] 9.4 用户确认第二份后，执行此前未见的教师/园务 Brief。
- [ ] 9.5 三份均保存 PPTX、网页截图、逐页 Office PNG、质量证据和费用账本。
- [ ] 9.6 两次独立硬编码扫描通过。
- [ ] 9.7 用户最终确认后才能标记 009 完成。

证据：三个独立验收目录和用户确认记录。

## 执行纪律

- 每个 Phase 先验证再进入下一 Phase。
- 最终验收开始后冻结代码；任何失败只展示证据，未经用户确认不得修复。
- 禁止为通过某一基准稿增加主题、标题、页码或页面角色分支。
- 禁止把外部 Brief、参考 PPT 或验收答案写入生产提示词。
- 禁止用新增测试掩盖原测试失败。
- 禁止在未生成三份真实成品前宣布完成。

