# 008 实施任务

## 阶段 A：协议与离线基线

- [ ] A1 将 008 领域协议和迁移边界写入 presentation-model。
- [ ] A2 增加 NarrativeArc、PageLink、CrossPageConstraint、DeckVisualGrammar、AssetIdentity 和 RenderEvidence Schema。
- [ ] A3 增加引用完整性、问题/揭晓状态和制作语言测试。
- [ ] A4 更新架构守卫，禁止参考文件名、业务坐标映射、模板克隆和固定四策略进入 008 生产路径。
- [ ] A5 增加 AST 级防硬编码检查、依赖方向检查和公开 API 注释检查。
- [ ] A6 实现统一错误码、失效传播矩阵和原子仓储测试。

验收：全部使用手写通用 fixture，不调用模型。

## 阶段 B：叙事与设计规划

- [ ] B1 重写 Narrative Planner 合约，一次返回大纲和 NarrativeArc。
- [ ] B2 实现 NarrativeValidator 和可见文案预算。
- [ ] B3 重写 Design Planner 合约，一次返回 VisualGrammar、PageIntent、CrossPageConstraint 和 AssetIdentity 草案。
- [ ] B4 实现字体、色彩、关系和媒体角色归一化。

验收：模拟模型覆盖课堂、家长沟通、园务汇报三类未知 Brief；失败不重试。

## 阶段 C：关系驱动构图

- [ ] C1 将固定四策略替换为组合语法特征。
- [ ] C2 实现 PageLink 组级求解和状态差异约束。
- [ ] C3 增加原生形状装饰层和 DeckVisualGrammar 解析。
- [ ] C4 实现自适应候选搜索、结构差异停止条件、可配置工程上限及 Deck 级选择。
- [ ] C5 增加可读性、关系清晰度、视觉锚点和轮廓多样性评分。
- [ ] C6 为每次候选选择生成 LayoutDecisionTrace，并验证关系谓词不能直接返回布局树。

验收：横版、竖版、方形画布均无固定坐标；问题/揭晓 fixture 保持共享对象且不泄露答案。

## 阶段 D：素材身份与图片角色

- [ ] D1 实现 AssetBundlePlanner 和唯一素材去重。
- [ ] D2 实现基础身份、复用和受控变体。
- [ ] D3 实现背景、场景、主体、透明抠图、细节和证据图的独立提示词策略。
- [ ] D4 实现分辨率、比例、alpha、留白和引用检查。
- [ ] D5 将素材缩略图纳入单次批量视觉检查。

验收：版式前图片调用为零；同一实体不重复生成；未放置素材调用为零。

## 阶段 E：高保真预览与导出

- [ ] E1 实现真实字体测量 Port。
- [ ] E2 统一网页、PNG 和 PPTX 的文字、裁剪、透明度和层级语义。
- [ ] E3 移除导出静默 shrink。
- [ ] E4 实现实际 PPTX RenderEvidence 和 Scene/PPTX 差异检查。
- [ ] E5 验证 PowerPoint 与 WPS。

验收：真实 PPTX 逐页渲染无溢出、错误换行和错误裁剪。

## 阶段 F：质量修复

- [ ] F1 增加跨页、素材身份、媒体角色、视觉锚点和真实渲染规则。
- [ ] F2 实现整稿联系表 + 高风险页高清图的一次视觉请求。
- [ ] F3 将问题转成 RepairConstraint。
- [ ] F4 重新求解失败页组并重新导出、渲染、检查。

验收：修复不是机械换候选；未受影响素材不重新生成；自动付费重试为零。

## 阶段 G：编辑器

- [ ] G1 缩放/旋转、多选、对齐和分布。
- [ ] G2 添加、替换和裁剪图片。
- [ ] G3 新增、复制、删除和排序页面。
- [ ] G4 主题编辑、单页重新设计、单素材重新生成。
- [ ] G5 PageLink 关联页保护和质量状态联动。

验收：所有操作支持 undo/redo，网页和导出读取同一 Scene Graph。

## 阶段 H：真实成品验收

- [ ] H1 离线 fixture、构建、类型检查、单测和集成测试全部通过。
- [ ] H2 生成一份原创幼儿课堂基准稿并逐页审查。
- [ ] H3 生成一份原创家长沟通基准稿并逐页审查。
- [ ] H4 生成一份原创教师/园务基准稿并逐页审查。
- [ ] H5 完成硬编码双重扫描、图片调用账单、PPTX/WPS 渲染和人工验收记录。
- [ ] H6 按 `verification.md` 执行失败注入、并发、缓存、断网、字体缺失、模型坏响应和导出差异测试。
- [ ] H7 审查公开协议、Port、求解约束、成本和错误处理代码的注释质量。

验收：三份真实成品全部满足 `spec.md`；在此之前不得宣布 008 完成。
